#include <stdio.h>
int remove(int arr[],int length){
    int temp[length];
    for (int i = 0; i < length; i++)
    {
        /* code */
    }
    
}
void main(){
    int arr[]={1,2,3,3,3,4,5,6,6,6,7,8,9,9};
    int length = sizeof(arr)/sizeof(arr[0]);
    remove(arr,length);
}